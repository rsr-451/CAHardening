CA VM USB Security Hardening Document
This document details the three critical Windows Registry settings used to completely disable USB device access on a high-security Certificate Authority (CA) Virtual Machine.

Run the following script in an elevated (Administrator) PowerShell session on the CA VM. A reboot is highly recommended after the script completes the configuration steps for the changes to fully take effect.

